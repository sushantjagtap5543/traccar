services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_DB=traccar
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - internal

  redis:
    image: redis:7-alpine
    restart: always
    command: redis-server --save 60 1 --loglevel warning --appendonly yes
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - redis_data:/data
    networks:
      - internal

  prometheus:
    image: prom/prometheus:latest
    restart: always
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    networks:
      - internal
    depends_on:
      admin-api:
        condition: service_healthy

  traccar:
    image: traccar/traccar:latest
    restart: always
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./traccar.xml.template:/app/traccar.xml.template:ro
      - ./entrypoint.sh:/app/entrypoint.sh:ro
    entrypoint: [ "/bin/sh", "/app/entrypoint.sh" ]
    command: [ "java", "-Xms512m", "-Xmx512m", "-jar", "tracker-server.jar", "/app/traccar.xml" ]
    ports:
      - "5000-5150:5000-5150"
      - "5000-5150:5000-5150/udp"
    networks:
      - internal

  admin-api:
    build:
      context: ./geosurepath-admin-api
    restart: always
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8083/api/health" ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - ./geosurepath-admin-api/logs:/app/logs
      - ./backups:/app/backups
    environment:
      - DATABASE_URL=postgres://postgres:${DB_PASSWORD}@db:5432/traccar
      - REDIS_URL=redis://redis:6379
      - ADMIN_API_KEY=${ADMIN_API_KEY}
    networks:
      - internal

  frontend:
    build:
      context: ./GeoSurePath
    environment:
      - VITE_ADMIN_API_KEY=${ADMIN_API_KEY}
      - VITE_ADMIN_API_URL=/api/admin
    depends_on:
      admin-api:
        condition: service_healthy
    networks:
      - internal

  nginx:
    build:
      context: ./nginx
    ports:
      - "80:80"
      - "443:443"
    networks:
      - internal
    healthcheck:
      test: [ "CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 3
    depends_on:
      frontend:
        condition: service_started
      admin-api:
        condition: service_healthy
      traccar:
        condition: service_started

  grafana:
    image: grafana/grafana-oss:latest
    restart: always
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-admin123}
      - GF_AUTH_ANONYMOUS_ENABLED=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    networks:
      - internal
    depends_on:
      - prometheus

networks:
  internal:
    driver: bridge

volumes:
  db_data:
  redis_data:
  grafana_data:
  prometheus_data:
