version: "3.9"

services:

  postgres:
    image: postgres:15-alpine
    container_name: traccar-postgres
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data

  backend:
    build: ./backend
    container_name: traccar-backend
    restart: always
    environment:
      TRACCAR_ADMIN_USER: ${TRACCAR_ADMIN_USER}
      TRACCAR_ADMIN_PASS: ${TRACCAR_ADMIN_PASS}
      SERVER_PORT: ${SERVER_PORT}
    depends_on:
      - postgres
    ports:
      - "${SERVER_PORT}:8082"

  api:
    build: ./services/api-server
    container_name: traccar-api
    restart: always
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      DB_NAME: ${POSTGRES_DB}
      JWT_SECRET: ${JWT_SECRET:-super-secret-key}
      REDIS_HOST: redis
    depends_on:
      - postgres
      - redis
    ports:
      - "3001:3001"

  redis:
    image: redis:alpine
    container_name: traccar-redis
    restart: always

  frontend:
    build: ./frontend/client-dashboard
    container_name: traccar-frontend
    restart: always
    ports:
      - "3000:80"

  admin-frontend:
    build: ./frontend/admin-dashboard
    container_name: traccar-admin-frontend
    restart: always
    ports:
      - "3002:80"

  nginx:
    image: nginx:latest
    container_name: traccar-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - backend
      - frontend
